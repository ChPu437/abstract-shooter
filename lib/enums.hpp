#pragma once

enum BulletType {
	BTYPE_NORMAL,	
	BTYPE_FLAME // FLAME 是自机狙
};
enum BulletRelation {
	BREL_FRIENDLY, BREL_HOSTILE
};
enum Direction {
	D_UP, D_DOWN, D_LEFT, D_RIGHT, D_NONE
};
enum PlayerType {
	PTYPE_DEFAULT
};
enum EnemyType {
	ETYPE_BOSS_REDWARM
};
